import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // Must match Android `applicationId` (see android/app/build.gradle)
  appId: 'HoliWorks.com',
  appName: 'HoliWorks',
  webDir: 'www'
};

export default config;
